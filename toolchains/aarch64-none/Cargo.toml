[package]
name = "toolchains-aarch64-none"
version = "0.1.0"
edition = "2024"

[dependencies]
lib-boards = { path = "../../lib/boards", features = ["rpi-3b1"] }

[profile.dev]
panic = "abort"
lto = true
opt-level = "s"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
